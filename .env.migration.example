# Scheduled-maintenance migration configuration. Copy to .env.migration only
# during an approved database maintenance operation, then protect the file.
# This is an owner credential; it must never be supplied to the app container.
DATABASE_URL="postgresql://migration_owner:replace-with-owner-password@postgres:5432/sana"
