## 1. Brand Asset Setup

- [x] 1.1 Copy the uploaded IOIV logo from `/Users/mahdi/Downloads/ioiv-logo.png.jpeg` into `public/` with a stable app asset name.
- [x] 1.2 Configure the app favicon/browser icon to use the IOIV logo asset.
- [x] 1.3 Update app metadata title from the old service name to `سامانه اعتبار سنجی سانا`.

## 2. Public And Admin Login Presentation

- [x] 2.1 Replace the public login page old service name with `سامانه اعتبار سنجی سانا`.
- [x] 2.2 Replace the public login page old heading, description, and three-step guidance with the supplied supplier evaluation copy.
- [x] 2.3 Render `02186122370` as a `tel:` link and keep `داخلی 3` visible in the support sentence.
- [x] 2.4 Add the IOIV logo above the applicant login card.
- [x] 2.5 Remove the requested applicant login-card heading and explanatory paragraph while keeping the login form functional.
- [x] 2.6 Update the admin login page old service name to `سامانه اعتبار سنجی سانا`.
- [x] 2.7 Add the IOIV logo above the admin login card.

## 3. Shared Panel Branding

- [x] 3.1 Replace the shared sidebar letter mark with the IOIV logo in `components/layout/AppShell.tsx`.
- [x] 3.2 Preserve the existing `سانا` brand text and the user/admin panel subtitle in the sidebar.
- [x] 3.3 Add or update CSS for login logos, sidebar logo sizing, spacing, and mobile-safe layout.

## 4. Consistency And Verification

- [x] 4.1 Search the codebase for `سامانه اعتبار سنجی نفت ایران (سانا)` and update remaining non-behavioral display copy where appropriate.
- [x] 4.2 Run formatting/lint/build checks required by the project.
- [x] 4.3 Verify the public login page, admin login page, user sidebar, admin sidebar, phone link, and favicon visually in a browser.
