# Phase 13 verification

720 tests/86files pass;27 new session cases plus58 existing login-boundary cases.
Build/types pass; lint only the existing public image warning. No schema/grant change.
Production Next browser on390/1440 verifies applicant/admin hostile top-level reset GET
preserves live session with no Set-Cookie, foreign formPOST403, GETlogout405, actual
visible logout form, refresh/back without restored authentication, deleted/malformed/
expired/absent recovery. Restricted SELECT denial produces actual Prisma/Next503 with
unchanged cookie/no Set-Cookie; finally-restored grants recover303. Inactive admin clears.
Screenshots show existing readable Persian destinations without horizontal overflow.
Cookie HttpOnly/Secure/Lax/root/30minute creation is unit-tested; browser uses signed
synthetic cookies over loopback. Actual HTTPS/proxy issuance remains Phase16 qualification.

Initial browser harness selected hidden desktop logout form at390px. Replaced with the
visible accessible button locator; both completed browser runs pass. No application fix
was required. DB permission fault is isolated and restored in finally. No real SMS,
payment, production access or facilities enablement. Launch remains NO-GO.
