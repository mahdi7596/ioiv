# Phase8 attempts

A1:82 Phase7 hashes match; baseline lineage, quota, retry, cleanup and guards traced. Independent design reviewer confirms failed attempts poison predecessor uniqueness/adjacency, historical cleanup guards, exclusive retry/expiry and ambiguous-commit compensation must be fixed together. No-progress count0.

A2: Fresh owned isolated PostgreSQL, 29 baseline migrations and restricted-role R7 reproduction passed. Draft 30th migration and generated client applied only in isolated source.
A3: Real storage suite first exposed malformed PDF fixture (corrected), then terminal expiry reason incompatible with existing status constraint (corrected without relaxing constraint). Latest run 11/12 passed; remaining manual retry fixture omitted clearing failureReason. Fixed fixture.
A4: Independent draft reviewer identified stale orphan snapshot race and completed/stale record starvation. Fixes and regression proofs pending; phase remains IN_PROGRESS. No-progress count0.

A5: Twelve actual-storage replacement tests passed. Draft production build and generated-route typecheck passed; full regression 558 passed, three stale cleanup mocks failed and were updated to new transaction semantics. Restricted M2 persistence/quota suite passed.
A6: Orphan regression exposed Prisma void-column deserialization (fixed by casting lock result), then a deliberately aged PENDING row retained by an earlier failed fixture correctly deferred global cleanup. Test setup now invokes real expiry for aged isolated leftovers. Reviewer timeout refinement freezes orphan names before acquiring lock; fresh metadata and PENDING checks happen under lock. Noneditable/stale retry candidates and completed tombstones are excluded. No-progress count0.

A7: Reviewer direct-runtime findings fixed: deferred success/current consistency, binding-time quota/editability, active quarantine tombstone denial, last-known UI wording. Clock fixture updated to flush deferred constraints before reenabling its owner-only trigger. Twelve→18→23→28 targeted cases passed, including five actual process kills, orphan removal after unattached crash, same-key FAILED/storage replay, and late reference denial after cleanup timeout.
A8: Fresh29→30 migration rehearsal passed, including atomic ambiguity abort and corrected rerun. Initial rehearsal harness used an incorrect trigger name; retained attempt1 log, corrected exact name. Restricted M2/R7 full150MiB suite and M1/M2/M6/M7/M8 SQL suites passed; constraints forced before rollback. Migration revised once more for synchronized legacy references and rehearsed again in phase8_migration3.
A9: Production390/1440 profile browser passed with controlled localINSTREAM, failed/unavailable replacement retention, owner/anonymous/foreign download checks, success/reload. Screenshot inspection found long messages rendered as columns; grouped recovery text and isolated filename direction. Final screenshot rerun pending.
A10: Full regression578 total:572 passed, six payment-fixture failures correctly rejected detached template PASSED rows. Fixed fixture to create successful file+current pointer in one owner transaction; payment behavior/constraints remain unchanged. Final verification uses a new labelled container and fresh full30 migrations, not incrementally edited draft schema. Draft environment/setup logs retained. No-progress count0.
A11: Final review caught PERFORM overwriting PL/pgSQL FOUND in completed-tombstone replay. Corrected existence-check order, added idempotent SUCCEEDED/detached replay. Applied exact final checksum8ea72e679cfa0820479d79eb25a075690289be2a33f038060a3c476a51d5c6e3 fromscratch30 migrations in another owned container; source/DB checksums match, restricted role and0 enabled facilities verified. Rehearsal4 passed. Restricted quota fixture now accepts either safe denial when active candidate has no stored bytes (content-required guard legitimately runs first).
A12: Final578/79 tests passed, including29 R7 +97 preserved restricted DB cases. Browser390/1440 passed and screenshots inspected, payment five process scenarios passed, types/lint/build passed. One preexisting public image warning. All19 checkpointed auth/payment/SMS runtime source hashes unchanged; payment fixture transaction changed only for new file atomicity constraint. Code reviewer approved. Final resource shutdown/evidence approval pending. No-progress count0.

A13: Final code/evidence reviewers approved.578/79/29R7 exact latest suite, sealed build/types/lint and browser pass.95 source/94 copy/104 pre-closure evidence hashes independently matched. All3 owned containers removed after ID/label checks,7 fixturePIDs absent/7ports closed,12abandoned synthetic roots removed. Closure documents updated and evidence manifest refreshed. Continue Phase9; no-progress count0.
